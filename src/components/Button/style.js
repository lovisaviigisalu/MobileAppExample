import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        marginTop:30,
        backgroundColor: "#4F63AC",
        paddingHorizontal: 8,
        paddingVertical: 20,
        borderRadius: 8,
        width:'100%'
    },
    title:{
        color: '#ffffff',
        textAlign: 'center',
        fontSize: 16,
        fontWeight: 'bold'
    }
})